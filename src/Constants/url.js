const root = "https://afternoon-anchorage-74547.herokuapp.com";
const urls = {
  imageURL: root + "/image",
  profileURL: root + "/profile",
  demoURL: root + "/demo",
  expURL: root + "/exp",
}

export default urls